namespace {{Name}};

using Microsoft.Accordant;

/// <summary>
/// Spec defines expected behavior for each operation.
/// The spec is the single source of truth for what "correct" means.
/// </summary>
public static class {{Name}}Spec
{
    public static Spec<{{Name}}State> Create()
    {
        var spec = new Spec<{{Name}}State>();

        // Define operations here
        // Example:
        // spec.Operation<CreateRequest, CreateResponse>("Create", (request, state) =>
        // {
        //     return Expect.That<CreateResponse>(r => r.Success)
        //                  .ThenState<{{Name}}State>(s => { /* update state */ });
        // });

        return spec;
    }
}
